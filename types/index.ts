export type Explore = {
  id: string;
  imgUrl: string;
  title: string;
};

export type Feature = {
  imgUrl: string;
  title: string;
  subtitle: string;
};

export type Insight = {
  imgUrl: string;
  title: string;
  subtitle: string;
};
